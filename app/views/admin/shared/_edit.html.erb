<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

<% end %>

<% if @edit %>
  <div><h3><%= _("Merge Articles") %></h3>
  <div><%= form_tag(admin_content_path) do %>
    <div><%= label_tag("merge_with", "Article ID") %></div>
    <div><%= text_field_tag("merge_with") %></div>
    <div><%= submit_tag("Merge") %></div>  
  <% end %>
  </div>
  </div>
<% end %>
